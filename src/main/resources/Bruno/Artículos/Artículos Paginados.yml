info:
  name: Artículos Paginados
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/api/articulos?page=2&size=4
  params:
    - name: page
      value: "2"
      type: query
    - name: size
      value: "4"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
